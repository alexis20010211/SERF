import { Solicitudes } from './solicitudes';

describe('SolicitudesComponent', () => {
  let component: Solicitudes;

  beforeEach(() => {
    component = new Solicitudes();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
